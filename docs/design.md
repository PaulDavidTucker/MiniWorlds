### Docs

This game follows a simple game loop located in game.c, with rendering and windows being handled by SDL in main.c. PLayer and world logic is handled in their respective files. Structs and data objects are defined in headers and operated by source files.

// Probably add more stuff here
